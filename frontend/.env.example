# Only needed if running `npm run dev` directly on the host, outside Docker.
# Points Vite's dev-server proxy (see vite.config.ts) at the backend.
# Inside Docker Compose this is set to http://backend:8000 automatically.
VITE_API_URL=http://localhost:8001
